for (auto it = pos.begin(); it != pos.end(); it++)
        {
            cout << *it << " ";
        }
        cout << endl;

        for (auto it = neg.begin(); it != neg.end(); it++)
        {
            cout << *it << " ";
        }
        cout << endl;